{
  "devDependencies": {
    "husky": "^3.0.5",
    "lint-staged": "^9.2.5",
    "npm-run-all": "^4.1.5"
  },
  "private": true,
  "scripts": {
    "fixpack": "npx fixpack",
    "serve": "docker-compose run --rm --service-ports serve",
    "server": "run-s serve",
    "sh": "docker-compose run --rm --service-ports sh"
  }
}
